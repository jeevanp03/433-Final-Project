export default function Dashboard() {
  return (
    <div>
      <h2 className="text-page-title mb-6">Dashboard</h2>
      <p className="text-body text-[var(--muted)]">
        KPI cards, forecast snapshot, and recommendations will go here.
      </p>
    </div>
  );
}
