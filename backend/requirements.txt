# =============================================================================
# requirements.txt
# Household Energy Dashboard – MSE 433 Final Project
#
# Install into a virtual environment:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Version pins: major versions locked, minor versions flexible (>=X.Y,<X+1.0).
# =============================================================================

# --- Data processing ----------------------------------------------------------
pandas>=2.0,<3.0
pyarrow>=14.0,<20.0
numpy>=1.26,<3.0

# --- Machine learning ---------------------------------------------------------
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0

# --- Statistics & time-series -------------------------------------------------
scipy>=1.12,<2.0
statsmodels>=0.14,<1.0

# --- Explainability -----------------------------------------------------------
shap>=0.45,<1.0

# --- Visualisation ------------------------------------------------------------
plotly>=5.20,<6.0

# --- Dashboard ----------------------------------------------------------------
streamlit>=1.35,<2.0

# --- Prescriptive optimisation ------------------------------------------------
pulp>=2.8,<3.0

# --- Configuration & environment ----------------------------------------------
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0

# --- UCI dataset fetch --------------------------------------------------------
ucimlrepo>=0.0.3

# --- Notebooks ----------------------------------------------------------------
jupyter>=1.0,<3.0
ipykernel>=6.0,<7.0

# --- Testing ------------------------------------------------------------------
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
